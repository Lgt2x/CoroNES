# CoroNES
NES Emulator written out of boredom during COVID-19 containment
Also my first C++ project, with the intent of learning the language.

For now, only the assembler is (kinda) functional. To compile, run
```shell script
mkdir build
cd build
cmake ..
cmake
```

then run with `./assembler`